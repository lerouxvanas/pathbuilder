RewriteEngine On

# If the request path ends with a / (indicating a directory), redirect to index.php
RewriteRule ^.*\/$ index.php [L,NC,R=302]


RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [L]
